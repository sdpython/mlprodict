image:
  - Visual Studio 2019
environment:
  matrix:
  - TOXENV: py39

init:
  - python -V
  - python3.9 -V

install:
  - python3.9 -m pip install wheel
  - python3.9 -m pip install numpy pandas scipy matplotlib scikit-learn
  - python3.9 -m pip install onnx onnxruntime --no-deps
  - python3.9 -m pip install -r requirements-win.txt --upgrade-strategy eager

build: off

before_test:
  - python3.9 -m -u setup.py build_ext --inplace

test_script:
  - python3.9 -m -u setup.py unittests -d 15 -g ".*((LONG)|(SKIP)|(notebooks)).*"

after_test:
  - python3.9 -m -u setup.py bdist_wheel

artifacts:
  - path: dist
    name: mlprodict
  - path: onnx\dist
    name: onnx

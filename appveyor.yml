image:
  - Visual Studio 2019
environment:
  matrix:
    - PYTHON: "C:\\Python310-x64"
      PYTHON_VERSION: "3.10.x"
      PYTHON_ARCH: "64"

install:
  - "%PYTHON% -m pip install wheel"
  - "%PYTHON% -m pip install numpy pandas scipy matplotlib scikit-learn"
  - "%PYTHON% -m pip install onnx onnxruntime"
  - "%PYTHON% -m pip install -r requirements-win.txt"

build: false

before_test:
  - "%PYTHON% -u setup.py build_ext --inplace"

test_script:
  - "%PYTHON% -u setup.py unittests -d 15 -g \".*((LONG)|(SKIP)|(notebooks)|(asv)|(plotting)).*\""

after_test:
  - "%PYTHONEXE% -u setup.py bdist_wheel"

artifacts:
  - path: dist
    name: mlprodict
  - path: onnx\dist
    name: onnx
